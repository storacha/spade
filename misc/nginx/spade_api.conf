log_format service_json_combined escape=json
'{'
  #'"accept_enc":"$http_accept_encoding"'
  '"timestamp":"$time_iso8601",'
  '"req_uuid":"$sent_http_x_spade_request_uuid",'
  '"http_status":$status,'
  '"fail_slug":"$sent_http_x_spade_failure_slug",'
  '"took_secs":$request_time,'
  '"sp":"$sent_http_x_spade_fil_spid",'
  '"sent_bytes":$bytes_sent,'
  '"compression":"$gzip_ratio",'
  '"req":"$request_method $host$request_uri",'
  '"req_bytes":$request_length,'
  '"remote_addr":"$remote_addr",'
  '"user_agent":"$http_user_agent"'
'}';

map $http_x_forwarded_proto $cur_scheme {
  default $http_x_forwarded_proto;
  ''      $scheme;
}

server {
  root /var/www/spade;

  server_name
    api.spade.storacha.network
  ;

  add_header Cache-Control "no-store, max-age=0";

  access_log /var/log/nginx/spade_access_log.json.log service_json_combined;

  real_ip_header X-Forwarded-For;
  real_ip_recursive on;
  # source https://www.cloudflare.com/ips/
  # https://www.cloudflare.com/ips-v4
  # https://www.cloudflare.com/ips-v6
  # ( curl -s https://www.cloudflare.com/ips-v4 ; echo ; curl -s https://www.cloudflare.com/ips-v6; echo ) | sed 's/.*/set_real_ip_from &;/'
  set_real_ip_from 173.245.48.0/20;
  set_real_ip_from 103.21.244.0/22;
  set_real_ip_from 103.22.200.0/22;
  set_real_ip_from 103.31.4.0/22;
  set_real_ip_from 141.101.64.0/18;
  set_real_ip_from 108.162.192.0/18;
  set_real_ip_from 190.93.240.0/20;
  set_real_ip_from 188.114.96.0/20;
  set_real_ip_from 197.234.240.0/22;
  set_real_ip_from 198.41.128.0/17;
  set_real_ip_from 162.158.0.0/15;
  set_real_ip_from 104.16.0.0/13;
  set_real_ip_from 104.24.0.0/14;
  set_real_ip_from 172.64.0.0/13;
  set_real_ip_from 131.0.72.0/22;
  set_real_ip_from 2400:cb00::/32;
  set_real_ip_from 2606:4700::/32;
  set_real_ip_from 2803:f800::/32;
  set_real_ip_from 2405:b500::/32;
  set_real_ip_from 2405:8100::/32;
  set_real_ip_from 2a06:98c0::/29;
  set_real_ip_from 2c0f:f248::/32;

  gzip on;
  gzip_types text/plain text/css application/json;
  gzip_proxied any;
  gzip_vary on;
  gzip_min_length 256;
  gzip_comp_level 9;

  error_page 301 302 307 308 /default_3xx_body.json;

  # Strip trailing /
  location ~ ^(.+)/$ { return 308 $cur_scheme://$host$1; }

  # sigh.
  location /favicon.ico {
    return 204;
    access_log     off;
    log_not_found  off;
  }

  # public/* is a-ok
  location ~ ^/public(?:/.*)?$ {
    allow all;
  }

  # legacy API redirects
  location /pending_proposals         { return 301 $cur_scheme://$host/sp/pending_proposals; }

  # only hit the app if we recognize the request
  location ~ ^/sp/(?:status|eligible_pieces|pending_proposals|piece_manifest|invoke)$ {

    # Because we are in a *non-default* server{...}, merge_slashes is not available to us to set
    # Do a hideous if-match instead, not to bombard the app with things it doesn't expect
    # `if` is safe here: https://www.nginx.com/resources/wiki/start/topics/depth/ifisevil/
    if ( $request_uri ~ "//" ) {
      error_page 403 /default_403_body.json;
      return 403;
    }

    # short-circuit 401 if header absent: do not even hit the app
    include /var/www/spade/unauth_short_circuit.conf;

    # special case these so that we output a placeholder at all times
    # especially important for 400, as golang's http error handler isn't json
    proxy_intercept_errors on;
    error_page 400 500 502 /default_app_error_body.json;

    proxy_set_header Accept-Encoding ""; # we are the ones compressing, not the app
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_pass http://127.0.0.1:8080;
  }

  # for everything else serve an unknwon
  location / {
    # short-circuit 401 if header absent
    include /var/www/spade/unauth_short_circuit.conf;
    error_page 403 /default_403_body.json;
    return 403;
  }

  # SSI for error messages
  location ~ ^/default_\w+_body.json$ {
    internal;
    allow all;
    ssi on;
    ssi_types *;
    add_header WWW-Authenticate: FIL-SPID-V0 always;
  }
}
